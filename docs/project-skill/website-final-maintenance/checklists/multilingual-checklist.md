# Multilingual Checklist

- `/en`, `/es`, `/ru`, `/ar`, `/fr`, `/pt` routes load.
- Localized product/news details load.
- Language switcher links point to the equivalent route.
- hreflang is reciprocal where practical.
- RTL route does not overflow.
- Forms work from localized pages.
